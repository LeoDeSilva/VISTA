![PYGAME];

newWindow(500,500);

[]int pos = [250, 250];
[]int prev = pos;

[]int offset() {
    return [rnd(-100,100), rnd(-100,100)];
}

void update() {
    clear();
    tick(1);
    prev = pos;
    pos = addVector2D(pos, offset());
    rect(prev, [50,50], RED);
    rect(pos, [50,50], BLACK);
}