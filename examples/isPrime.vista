newWindow(500,500);

bool isPrime(int n) {
    for (int i : 2 => n) {
        if (n % i == 0) {
            return false;
        }
    }
    return true;
}

Vector2D position = newVector2(0,0);

int i = 9;
float n = 0.5;
string s = "hello world";

if (isPrime(i)) {
    text(str(i) + "is prime", position);
} else {
    text(str(i) + "is not prime", position);
}