[]int ages = [1,2,3,4];
[][]int heights = [[1,2], [3,4]];
[][][]int ints = [[[1,2]],[[3,4]]];

ages[0] = -1;
heights[0][1] = 1000;
ints[1][0][0] = 100;

ages = append(ages, 10);
ages = remove(ages,1);
ages = insert(ages,1,-100);

print("ages", ages);
print("heights", heights);
print("ints",ints);

[]int indices = [];
for (int i => range(10)) {
    indices = append(indices, i);
}
print(indices);